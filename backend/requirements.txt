# ── 基础运行依赖（轻量，保证“装完即可跑” MVP 全链路）──────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
numpy==2.2.1
python-dateutil==2.9.0.post0
httpx==0.28.1            # OpenAI 兼容 LLM 客户端 + TestClient

# ── 测试 ──────────────────────────────────────────────────────
pytest==8.3.4

# ── 认知诊断升级：PyTorch 版 NeuralCD + DKT 联合训练 ─────────────
# 安装后运行 scripts/train_diagnosis.py，引擎将自动从 NumPy+BKT 升级为联合模型；
# 不安装则自动回退 NumPy+BKT，全部功能照常可用。
torch>=2.2            # CPU 版即可：pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── 可选：中文分词（缺失时自动回退到字符 n-gram，不影响运行）──────
# jieba==0.42.1

# ── 生产替换点（MVP 不强制安装；启用真实后端时再装）────────────────
# transformers==4.48.0    # 教育大模型微调 / 本地推理
# pymilvus==2.5.0         # 向量库（替换 HashingEmbedder + 内存检索）
# nebula3-python==3.8.0   # 知识图谱（替换内存图）
# FlagEmbedding==1.3.3    # bge-m3 / bge-reranker-large
